<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Search Edit Text Field. -->
    <style name="searchEditText" parent="AppTheme">
        <item name="android:layout_width">@dimen/width_0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">7</item>
        <item name="android:background">@color/color_light_gray</item>
        <item name="android:padding">@dimen/padding_10dp</item>
        <item name="android:textColorHint">@color/color_light_blueGray</item>
    </style>

    <!-- Search Image Button. -->
    <style name="searchButton" parent="AppTheme">
        <item name="android:layout_width">@dimen/width_0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:padding">@dimen/padding_6dp</item>
    </style>

    <!-- Temporary message for the user to start with. -->
    <style name="mediumTextView" parent="AppTheme">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">@dimen/padding_6dp</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
        <item name="android:textColor">@color/color_blueGray</item>
    </style>

    <!-- Title of the book TextView. -->
    <style name="textViewBook" parent="AppTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:maxLines">3</item>
        <item name="android:textColor">@color/color_dark_blueGray</item>
        <item name="android:textSize">@dimen/textSize_title</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:layout_gravity">top</item>
    </style>

    <!-- Subtitle of the book TextView. -->
    <style name="textViewBook.subtitle">
        <item name="android:maxLines">2</item>
        <item name="android:textSize">@dimen/textSize_subtitle</item>
        <item name="android:textColor">@color/color_light_blueGray</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Author of the book TextView. -->
    <style name="textViewBook.author">
        <item name="android:layout_gravity">bottom</item>
        <item name="android:textSize">@dimen/textSize_subtitle</item>
        <item name="android:textColor">@color/color_blueGray</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>
